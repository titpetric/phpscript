| Example | What it does |
|---|---|
| `phpscript list .` | A markdown inventory of the routes, files and classes of one tree. |
| `phpscript list --stdlib` | The functions, classes and constants the runtime registers, instead of scanning a tree. |
