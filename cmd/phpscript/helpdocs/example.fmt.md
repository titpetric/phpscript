| Example | What it does |
|---|---|
| `phpscript fmt ./...` | Rewrites every php file below the current directory in the canonical form. |
| `phpscript fmt -l ./...` | Names the files that would change and rewrites nothing, which is the CI check. |
